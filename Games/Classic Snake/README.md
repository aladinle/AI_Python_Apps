# AI Python Apps

This project now includes a classic Snake game built with Tkinter, so it runs with standard Python and no extra game library.

## Run the Snake game

```bash
python snake_game.py
```

Controls:

- Arrow keys to move
- Space to start or pause
- R to restart after a crash

## Run tests

```bash
pytest -v
```
